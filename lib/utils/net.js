'use strict';
const realTimePrice = require('../realTimePrice');
const symbolUtil = require('./symbol');

let net = new class{

    /**
     * 检测套接字的连接情况
     * @param {String} [site] 
     * @param {String} [clientType]
     */
	checkClientConnect(site,clientType){

    } 

}();

module.exports = net;